format_version: 10
pipelines:
  tools-create:
    group: Terraform
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-a66247d:
        git: https://github.com/poojachiru04/tools-automation
        shallow_clone: true
        auto_update: true
        branch: main
    secure_variables:
      SSH_PASSWORD: "AES:tSqXqpp11OV5Z6PcJPidrQ==:jC4ih8+cvxnKZSnGZ0TAQA=="
    stages:
      - create:
          fetch_materials: true
          environment_variables: { env: dev }
          approval: {type: manual}
          jobs:
            terraform:
              tasks:
                - exec: { command: terraform , arguments: [ "init"] }
                - exec: { command: terraform , arguments: [ "apply", "-auto-approve"] }
